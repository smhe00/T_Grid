# User Escalation — G1-T006（RESOLVED）

Resolved at: `2026-08-14T21:24:58+08:00`

用户已明确授权真实只读验收并禁止任何下单和撤单。架构师根据用户指定的 reverse_repo 位置，选择
simulation 环境并完成 hashed-binding 脱敏 preflight；任务已切换 `CLAUDE_READY`。下方为历史要求。

## Why User Input Is Required

G1-T001 至 G1-T005 已完成环境静态盘点、三个离线只读 Adapter 与离线集成探针。Gate 1 设计验收的剩余
条件必须连接用户真实 MiniQMT 并读取账号/行情；这涉及用户账号边界与本地路径，不能由 Agent 猜测或
自动发现。

## Required Response

请用户：

1. 明确授权：`授权 G1-T006 真实 MiniQMT 只读验收；禁止任何下单和撤单。`
2. 启动并登录 MiniQMT。
3. 创建被 Git 忽略的 `config/gate1_qmt.local.yaml`，内容格式见 `work/control/CURRENT_TASK.md`。
4. 仅回复“配置已就绪”；不要在聊天或受版本控制文件中粘贴账号 ID。

未同时满足以上条件前，不执行任何真实连接或查询。
