# Gate 0 / Task G0-T001

当前任务的唯一规范正文为：

```text
work/control/CURRENT_TASK.md
```

任务名称：**项目骨架与配置安全基础**。

本任务只实现可靠、fail-closed 的配置基础；SQLite、logging、CLI、Event Queue 和最终 `docs/GATE_0_REPORT.md` 留给后续 Gate 0 子任务。完成后必须进入 `REVIEW_READY`，不得自行继续。

